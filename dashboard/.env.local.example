# =============================================================================
# ENVIRONMENT VARIABLES
# =============================================================================
# Local Development: Copy this file to .env.local and fill in the values
#
# Cloudflare Deployment:
#   1. Go to: Cloudflare Dashboard → Workers & Pages → [Your Project] → Settings
#   2. Click "Variables and Secrets" (under Settings)
#   3. Add each variable below (use "Encrypt" for sensitive values like keys)
#   4. Make sure to add for both Production AND Preview environments
#   5. Redeploy after adding variables
#
# CLI Alternative (for secrets):
#   npx wrangler secret put STRIPE_SECRET_KEY
#   npx wrangler secret put SUPABASE_SERVICE_ROLE_KEY
#   etc.
# =============================================================================

# Supabase Configuration (Central Database)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Server-side only (keep secret!) - Use "Encrypt" in Cloudflare
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Encryption key for API keys (generate with: openssl rand -base64 32)
ENCRYPTION_KEY=your-32-byte-base64-encryption-key

# PostHog Analytics (get from https://eu.posthog.com/settings/project)
NEXT_PUBLIC_POSTHOG_KEY=phc_your_project_api_key
NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

# Stripe Configuration (get from https://dashboard.stripe.com/apikeys)
# Use "Encrypt" in Cloudflare for these sensitive values
STRIPE_SECRET_KEY=sk_live_your_stripe_secret_key
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Price IDs (create products in Stripe Dashboard -> Products)
# Each plan has a base price and a GEO-optimized price
# Get these from: Stripe Dashboard → Products → [Product] → Pricing → Copy price ID
STRIPE_PRICE_STARTER_BASE=price_1xxxxx
STRIPE_PRICE_STARTER_GEO=price_1xxxxx
STRIPE_PRICE_PRO_BASE=price_1xxxxx
STRIPE_PRICE_PRO_GEO=price_1xxxxx
STRIPE_PRICE_BUSINESS_BASE=price_1xxxxx
STRIPE_PRICE_BUSINESS_GEO=price_1xxxxx
